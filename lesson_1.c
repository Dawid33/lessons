#include <stdio.h>

main() {                  // Define the fuction main.
    int x = 0;            // Create a variable 'x' with the number 0 stored inside it.
    x = 30;               // Assign the value 30 to the variable
    printf("%d\n", x);    // Print the value inside the variable to the screen  
}